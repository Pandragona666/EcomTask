<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="http://prismlibrary.com"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="EcomLocatorV2.Views.UserDetail">

    <StackLayout>
        <StackLayout>
            <Label Text="{Binding User.FirstName}" TextColor="Indigo" FontSize="15" FontAttributes="Bold"/>
            <Label Text="{Binding User.LastName}" TextColor="Indigo" FontSize="15" FontAttributes="Bold"/>
            <Label Text="{Binding User.Age}" TextColor="Indigo" FontSize="15" FontAttributes="Bold"/>
            <Label Text="{Binding User.City}" TextColor="Indigo" FontSize="15" FontAttributes="Bold"/>
        </StackLayout>
    </StackLayout>
    
</ContentPage>